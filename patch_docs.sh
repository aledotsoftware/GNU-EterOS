sed -i 's/- \*\*Action (Pending):\*\* Upgrade the Binder routing mechanism from the basic static payload-copy shim to proper memory mapping for transaction data transfer./- \*\*Action (Completed):\*\* Upgrade the Binder routing mechanism from the basic static payload-copy shim to proper memory mapping for transaction data transfer./' docs/ANDROID_ROADMAP.md
sed -i 's/2\. \*\*Binder Mmap\*\*: Implement `mmap` for `\/dev\/binder` to handle transaction buffers directly via VMA, replacing the current `kmalloc` \/ `anonymous page` workaround./2. **Binder Mmap**: (Completed) Implement `mmap` for `\/dev\/binder` to handle transaction buffers directly via VMA./' docs/android_compat_gap.md
