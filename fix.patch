--- kernel/shell/cmd_net.c
+++ kernel/shell/cmd_net.c
@@ -35,11 +35,11 @@
         terminal_write_string(buf);
     } else {
         terminal_write_string("State: DOWN (Link or DHCP pending)\n");
-        terminal_write_string("\n\n\n");
     }
-    // Pad output to always be exactly 6 lines after the title
+
+    // Ensure exactly 6 output lines following the title
     if (!network_ready) {
-        // Output already has 1 line (MAC) + 1 line (State) + 3 newlines = 5 lines. Add 1 more.
+        // We only emitted MAC and State: DOWN, so we need 4 blank lines to reach 6 total.
+        terminal_write_string("\n\n\n\n");
-        terminal_write_string("\n");
     }
 }
--- kernel/shell/cmd_panel.c
+++ kernel/shell/cmd_panel.c
@@ -512,11 +512,12 @@
                 cmd_clear("");
                 terminal_write_string("\n  -- Red y Conectividad --\n");
                 cmd_net("");
-                // cmd_net outputs exactly 7 lines everywhere, so options start at y=10
+                // cmd_net outputs exactly 8 lines everywhere (1 title + 7 lines), so options start at y=10
                 terminal_write_string("\n  1. Renovar DHCP\n");
                 terminal_write_string("  2. Probar conexion (wget tudexgames.com)\n");
                 terminal_write_string("\n  Elija [1-2] o ESC para volver.\n");
                 char c = 0;
+                int start_y = 11;
                 while (1) {
                     terminal_set_cursor(panel_mouse_x, panel_mouse_y);
                     if (keyboard_has_input()) {
@@ -525,9 +526,9 @@
                     }
                     if (panel_mouse_clicked) {
                         panel_mouse_clicked = false;
                         if (panel_mouse_x >= 2 && panel_mouse_x <= 50) {
-                            if (panel_mouse_y >= 10 && panel_mouse_y <= 11) { // 7 lines from cmd_net + title
-                                c = '1' + (panel_mouse_y - 10);
+                            if (panel_mouse_y >= start_y && panel_mouse_y <= start_y + 1) { // 7 lines from cmd_net + title + newline
+                                c = '1' + (panel_mouse_y - start_y);
                                 if (c > '2') c = '2';
                                 break;
                             } else {
--- kernel/drivers/net/e1000.c
+++ kernel/drivers/net/e1000.c
@@ -319,7 +319,6 @@
         }
     }

-    serial_write_string("[NET] Paquete enviado.\n");
     return 0;
 }
