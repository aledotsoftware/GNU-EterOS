--- tests/test_rtc.c
+++ tests/test_rtc.c
@@ -35,6 +35,11 @@

 // Include source file to test
 #include "../kernel/drivers/rtc/rtc.c"
+
+/* Assert might be undefined in strict environments if stdlib isn't fully linked or if standard asserts conflict */
+#ifndef assert
+#define assert(x) do { if (!(x)) { printf("ASSERT FAILED: %s\n", #x); exit(1); } } while(0)
+#endif

 void test_rtc_bcd_24h() {
     printf("Testing RTC BCD 24h...\n");
