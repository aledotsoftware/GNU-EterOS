1. **Fix `fat32.c` errors**
   Replace remaining generic `-4` and `-5` error codes to `-EIO` in `fat32.c`.

2. **Pre-commit checks**
   Ensure proper testing, verification, review, and reflection are done before committing.

3. **Submit**
   Once everything is clean and tests pass, commit and submit.
